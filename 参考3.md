下面这部分其实是 **微服务协作最核心的一环**：

> 99% 团队微服务失败，就是因为 proto 没治理。

这部分我给你一套 **企业真实可执行的协议治理体系**。

---

# 一、api-contracts 仓库定位

它不是普通 repo，而是：

**整个公司所有微服务通信的唯一协议源**

任何服务之间：

* gRPC
* HTTP
* MQ message

全部来自这个仓库。

它的地位 ≈ 数据库 schema 仓库。

---

# 二、仓库目录设计（必须强约束）

```text
api-contracts/
│
├── buf.yaml
├── buf.gen.yaml
├── Makefile
│
├── api/
│   ├── user/
│   │   └── v1/
│   │       ├── user.proto
│   │       └── user_http.proto
│   │
│   ├── order/
│   │   └── v1/
│   │       └── order.proto
│   │
│   └── payment/
│       └── v1/
│           └── payment.proto
│
└── third_party/
    ├── google/api
    └── validate
```

关键点：

### 1️⃣ 按“领域”分目录，不按服务名

因为服务可以拆分，但领域不会。

例：

* user 域
* order 域
* payment 域

不是：

* user-service
* user-bff
* user-admin

否则未来拆服务时 proto 全乱。

---

# 三、proto 包名规范（极其重要）

每个 proto 必须固定三段：

```proto
package company.user.v1;
```

结构：

| 段       | 含义     |
| ------- | ------ |
| company | 公司命名空间 |
| user    | 领域     |
| v1      | 大版本    |

生成 go 包：

```proto
option go_package = "company.com/api-contracts/api/user/v1;v1";
```

这样所有服务引用路径永远一致。

---

# 四、proto 文件拆分规范

每个领域固定三种文件：

```text
user/
 └── v1/
     ├── user.proto        # message 定义
     ├── user_grpc.proto   # gRPC service
     └── user_http.proto   # HTTP 映射
```

### message 单独放的原因

避免服务间循环依赖。

---

# 五、版本治理（重点中的重点）

proto 只允许 **大版本目录升级**

### 正确版本演进：

```text
user/v1/
user/v2/
```

永远不要：

```text
user/v1.1 ❌
user/v1beta ❌
```

原因：Go module + import 路径必须稳定。

---

# 六、字段变更铁律（必须背下来）

proto 是 **向前兼容协议**。

### 永远允许

```proto
message User {
  int64 id = 1;
  string name = 2;
  string avatar = 3; // 新增字段 ✔
}
```

### 永远禁止

#### ❌ 修改字段类型

```proto
int64 id = 1;  → string id = 1
```

#### ❌ 修改字段编号

```proto
string name = 2; → = 3
```

#### ❌ 删除字段

```proto
string name = 2;  // 删除 ❌
```

只能废弃：

```proto
string name = 2 [deprecated = true];
```

---

# 七、什么时候升 v2 ？

只有 3 种情况允许升大版本：

1️⃣ 修改字段类型
2️⃣ 删除字段
3️⃣ 修改接口语义

例：

```proto
rpc GetUser → rpc GetUserProfile
```

这叫 **破坏性变更**。

必须创建：

```text
user/v2
```

v1 永远保留。

---

# 八、proto 发布流程（企业流程）

## 流程图

```text
开发 → PR → 合并 → 打 tag → 服务升级
```

---

## 1️⃣ 修改 proto → 提 PR

任何人不能直接 push main。

PR 必须经过：

* 架构负责人 review
* buf breaking check

---

## 2️⃣ 自动破坏性检测（buf）

buf.yaml：

```yaml
breaking:
  use:
    - FILE
```

CI 自动检查：

* 是否删除字段
* 是否修改编号
* 是否破坏兼容

CI 不通过 → 不允许合并。

---

## 3️⃣ 发布 tag（关键）

合并后：

```bash
git tag v1.5.0
git push --tags
```

这一步非常关键。

proto **必须版本化发布**。

---

# 九、服务如何升级协议

服务 go.mod：

```go
require company.com/api-contracts v1.5.0
```

升级协议：

```bash
go get company.com/api-contracts@v1.6.0
go mod tidy
```

是否升级由服务自己决定。
这就是 **真正解耦**。

---

# 十、为什么这套机制非常关键

没有版本治理会发生：

| 事故        | 结果     |
| --------- | ------ |
| A 改 proto | B 编译失败 |
| B 发布服务    | 线上全挂   |
| 团队互相等待    | 开发效率崩溃 |

有版本治理：

| 行为            | 影响          |
| ------------- | ----------- |
| proto 发布 v1.6 | 老服务继续用 v1.5 |
| 新服务用 v1.6     | 不影响旧服务      |

这才是真正微服务。

---

# 十一、接口设计规范（避免未来爆炸）

### 必须统一返回结构

```proto
message Reply {
  int32 code = 1;
  string message = 2;
}
```

不要让每个服务自定义错误结构。

---

### 必须支持分页标准

```proto
message PageRequest {
  int32 page = 1;
  int32 page_size = 2;
}
```

---

### 必须支持 trace id 透传

```proto
message Metadata {
  string trace_id = 1;
}
```

---

# 十二、CI 模板（proto 仓库）

```yaml
stages:
  - lint
  - breaking

lint:
  script:
    - buf lint

breaking:
  script:
    - buf breaking --against '.git#branch=main'
```

---

# 十三、最终总结

真正的微服务协作基石：

| 不是   | 而是   |
| ---- | ---- |
| 框架   | 协议   |
| 服务数量 | 版本治理 |
| 技术能力 | 变更流程 |

---

如果你继续往企业级走，下一步就是：
👉 **统一错误码体系设计（跨服务 error code 规范）**。
