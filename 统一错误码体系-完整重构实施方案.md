# 统一错误码体系完整重构实施方案（不考虑兼容性）

本文档基于 `docs/统一错误码体系设计.md`，结合当前仓库现状（`template-proto`、`kratos-template`、`kratos-gateway`、`kratos-infra`）给出一套一次性到位的重构方案。  
目标是建立单一错误契约、统一错误语义和统一观测口径，并确保后续新增服务可直接复用。

---

## 1. 设计目标与范围

### 1.1 核心目标

- 错误码统一来源，避免重复定义和语义冲突
- HTTP、gRPC、Gateway 对外错误行为一致
- 业务错误与技术错误分层，减少跨层耦合
- 日志、指标、告警以错误码为核心维度
- 新增错误走标准流程，防止体系回退

### 1.2 适用范围

- `template-proto`：错误契约与错误码字典源头
- `kratos-template`：业务服务错误定义、映射与输出
- `kratos-gateway`：上游错误归一与对外输出
- `kratos-infra`：通用技术错误分类、转换与中间件支撑

---

## 2. 统一原则（强约束）

- 单一事实源：错误码只在 `template-proto/common/v1/errors.proto` 定义
- 禁止魔法值：禁止直接 `errors.New(123456, ...)` 或手写 reason 字符串
- 分层归责：业务错误在服务内部定义，基础设施层不承载业务域语义
- 对外稳定：`code` 和 `reason` 一旦发布不可复用，只能废弃
- 未知错误兜底：统一映射到 `INTERNAL_ERROR`

---

## 3. 错误码模型与编号策略

### 3.1 编号模型

采用 6 位数字编码：`AA BB CC`

- `AA`：领域（系统级）
- `BB`：模块
- `CC`：具体错误

示例：`200101`

- `20` 用户域
- `01` 登录模块
- `01` 用户不存在

### 3.2 领域分配（初版）

- `10xxxx`：gateway
- `20xxxx`：user
- `30xxxx`：order（预留）
- `40xxxx`：payment（预留）
- `80xxxx`：admin（预留）
- `90xxxx`：infra / system

### 3.3 reason 命名规范

- 全大写蛇形命名：`USER_NOT_FOUND`
- 前缀建议：`GATEWAY_`、`USER_`、`SYSTEM_`
- 语义唯一：同一语义仅允许一个 reason

---

## 4. 契约与目录落位

### 4.1 契约文件（必须）

- 新增：`template-proto/common/v1/errors.proto`
- 作用：定义 `enum ErrorReason` 与错误码注释

### 4.2 生成产物（只读）

- `kratos-template/api/common/v1/*`（由 proto 生成）
- 其他服务按各自生成流程拉取同一契约

### 4.3 服务内错误包

在 `kratos-template` 新增：

- `internal/errorsx/codes.go`：reason 常量绑定（引用 proto）
- `internal/errorsx/business.go`：业务错误工厂
- `internal/errorsx/system.go`：系统错误工厂
- `internal/errorsx/mapper.go`：技术错误到标准错误映射

在 `kratos-gateway` 新增：

- `internal/errorsx/mapper.go`：上游服务错误归一映射
- `internal/errorsx/http_encoder.go`：统一 HTTP 错误编码器

在 `kratos-infra` 新增/改造：

- `errorsx/classifier.go`：超时、取消、网络、依赖错误分类
- `errorsx/reason.go`：通用 reason 常量（仅通用，不含业务域）
- `middleware/*` 使用标准错误返回，不再返回裸哨兵 `error`

---

## 5. 错误类型分层与职责

### 5.1 业务错误（Domain Error）

- 来源：`biz` 层校验与业务规则
- 示例：`USER_NOT_FOUND`、`USER_NAME_INVALID`
- 要求：由 `internal/errorsx/business.go` 统一构造

### 5.2 技术错误（System Error）

- 来源：数据库、缓存、远程调用、序列化等
- 示例：`SYSTEM_DB_ERROR`、`SYSTEM_CACHE_ERROR`、`SYSTEM_REMOTE_TIMEOUT`
- 要求：先保留原始错误，再由 `mapper.go` 映射为统一错误

### 5.3 边界转换职责

- `data` 层：返回原始 err（可 wrap）
- `service` 层：统一调用 `errorsx.Map(err)`
- `server` 层：统一 ErrorEncoder 输出
- `gateway`：禁止直接透传上游 raw error，必须归一化

---

## 6. 协议输出规范（HTTP / gRPC）

### 6.1 推荐对外错误结构

```json
{
  "code": 200101,
  "reason": "USER_NOT_FOUND",
  "message": "user not found",
  "metadata": {
    "trace_id": "xxx",
    "service": "kratos-template"
  }
}
```

### 6.2 HTTP 状态码映射（推荐语义化）

- 参数错误：400
- 未认证：401
- 无权限：403
- 未找到：404
- 冲突：409
- 限流：429
- 系统错误：500/502/503/504

说明：业务 code 继续保留在 body 中，前端仍可按 `code/reason` 分支处理。

### 6.3 gRPC 映射

- 通过 `kerrors` 统一表达
- `code/reason/message` 与 HTTP 保持同源
- metadata 通过 details 或日志补充

---

## 7. 当前仓库的具体改造点

### 7.1 `template-proto`

1. 新增 `common/v1/errors.proto`
2. 维护 `ErrorReason` 枚举与码位注释
3. 生成代码并同步下游服务依赖

### 7.2 `kratos-template`

1. 新增 `internal/errorsx` 全套文件
2. 替换 `internal/biz/user.go` 中散落 `kerrors.BadRequest("INVALID_...")`
3. 替换 `internal/service/user.go` 中散落 reason 字符串
4. 改造 `internal/server/http_codec.go`：统一 ErrorEncoder
5. 将 `gorm`/`redis`/`grpc` 等错误统一走 `errorsx.Map`

### 7.3 `kratos-gateway`

1. 增加网关层统一错误编码与映射
2. 对上游返回错误进行 reason 白名单映射
3. 保证网关返回结构与业务服务一致

### 7.4 `kratos-infra`

1. 逐步废弃 `kratos-infra/errors/errors.go` 中裸哨兵错误对外使用
2. 中间件（auth/recovery）改为返回标准 `kerrors`
3. 沉淀技术错误分类函数供各服务复用

---

## 8. 编码规范（必须遵守）

### 8.1 代码中允许的错误创建方式

- 允许：`errorsx.UserNotFound(...)`
- 允许：`errorsx.Map(err)`
- 禁止：`kerrors.BadRequest("INVALID_XXX", "...")` 直接硬编码 reason
- 禁止：`errors.New("unauthorized")` 作为对外业务错误

### 8.2 message 规范

- 对外 message：简洁、可读、避免泄露内部实现
- 内部日志 message：可携带原始错误上下文

### 8.3 metadata 规范

至少包含：

- `trace_id`
- `service`
- `method`

可选：

- `upstream`
- `dependency`
- `retryable`

---

## 9. 日志、监控与告警设计

### 9.1 日志字段

错误日志必须包含：

- `trace_id`
- `error_code`
- `reason`
- `http_status` 或 `grpc_code`
- `service`
- `method`

### 9.2 指标

建议新增：

- `app_error_total{service,reason,code}`
- `app_error_total{service,http_status}`
- `app_dependency_error_total{service,dependency,reason}`

### 9.3 告警策略

- `INTERNAL_ERROR` 突增告警
- `SYSTEM_REMOTE_TIMEOUT` 持续高于阈值告警
- 单接口 `4xx/5xx` 比例异常告警

---

## 10. CI 与治理机制

### 10.1 CI 检查项

- 检查新增 reason 是否存在于 `errors.proto`
- 检查 reason 是否重复/冲突
- 扫描代码中是否存在硬编码 reason 字符串

### 10.2 代码评审规则

- 业务 PR 新增错误必须同步更新 `errors.proto`
- 无错误码定义的错误改动禁止合入
- 任何“未知错误直接透传”必须驳回

---

## 11. 分阶段实施计划（4 周）

### Phase 1（第 1 周）：契约与骨架

- 完成 `errors.proto`
- 落地 `kratos-template/internal/errorsx` 框架
- 完成基础 mapper（context/gorm/redis/grpc 常见错误）

### Phase 2（第 2 周）：业务服务收敛

- 替换 `kratos-template` 现有业务错误写法
- 改造 `http_codec` 统一输出
- 联调前端错误处理分支

### Phase 3（第 3 周）：网关与 infra 收敛

- 网关统一错误映射
- `kratos-infra` 中间件统一返回标准错误
- 清理旧错误哨兵的对外路径

### Phase 4（第 4 周）：观测与治理

- 接入指标与告警
- 增加 CI 规则
- 完成文档与团队流程固化

---

## 12. 验收清单

- [ ] 所有服务对外错误 `code/reason` 来源统一
- [ ] HTTP 与 gRPC 同语义错误表现一致
- [ ] 代码中无新增硬编码 reason 与 magic number
- [ ] 网关不透传上游 raw error
- [ ] 日志可按 `reason` 聚合，指标可按 `code` 统计
- [ ] 告警规则可识别关键错误激增

---

## 13. 风险与规避

- 风险：重构期 reason 迁移不完整导致前端分支错乱  
  规避：按服务维度分批切换，统一回归清单

- 风险：网关与服务错误结构不一致  
  规避：共享同一编码器协议与契约包

- 风险：团队后续回退为硬编码  
  规避：CI + Code Review 双重治理

---

## 14. 交付物清单

- `template-proto/common/v1/errors.proto`
- `kratos-template/internal/errorsx/*`
- `kratos-gateway/internal/errorsx/*`
- `kratos-infra/errorsx/*`
- 监控面板与告警规则配置
- 本实施文档（`docs/统一错误码体系-完整重构实施方案.md`）

---

## 15. 最终决策摘要

- 错误码字典统一到 proto，禁止多源定义
- `kratos-infra` 只承载通用技术错误能力，不承载业务域错误
- 服务内部通过 `errorsx` 统一创建和映射错误
- 网关与服务输出协议统一，日志与监控统一按 `code/reason` 观测

该方案可直接作为跨仓重构执行基线。
