# 统一错误码体系完整重构实施方案 v2（融合行业最佳实践）

本版本在 v1（`docs/统一错误码体系-完整重构实施方案.md`）基础上迭代，并参考以下业界规范/工程实践：

- Kratos 官方错误协议与 `protoc-gen-go-errors` 生成器
- Google AIP-193（Canonical Error Handling）
- gRPC `status.proto` 与 `google.rpc.ErrorInfo/BadRequest/RetryInfo`
- RFC 7807《Problem Details for HTTP APIs》
- Stripe / GitHub API 错误协议（类型化错误 + request_id + docs_url）
- Google SRE 错误预算 / SLO 工程化
- 12 Factor：日志结构化 / 可观测性

适用仓库：`template-proto`、`kratos-template`、`kratos-gateway`、`kratos-infra`。  
本方案不考虑兼容性，按一次性到位的最佳实践执行。

---

## 1. 设计目标（v2）

### 1.1 四个硬性目标

- 单一契约：错误码与 reason 只在一处定义，代码按契约自动生成
- 双协议同源：HTTP 与 gRPC 使用相同的 code/reason，外观可语义化但语义同源
- 生产可观测：日志、指标、告警、SLO 皆可按 code/reason 聚合
- 治理可持续：CI 检测冲突与冻结，错误码只增不改不删不复用

### 1.2 五个体验目标

- 前端可基于 reason 稳定分支，支持 i18n
- 网关/客户端可基于 metadata 自动决定是否重试
- 表单类错误可结构化返回多字段问题
- 安全：对外 message 不泄露实现细节
- 开发者写错误像写函数：强类型、IDE 可补全、单元测试可断言

---

## 2. 错误码模型（v2 升级）

### 2.1 六位编码 + 领域段

编码格式：`AABBCC`

- `AA` 领域：`10` gateway / `20` user / `30` order / `90` infra
- `BB` 模块：领域内子模块
- `CC` 具体错误：递增编号

领域段注册表集中在 `template-proto/common/v1/domains.yaml`，新增领域必须 PR 审批。

### 2.2 与 Kratos / gRPC 的对应

- `code`：业务 6 位码（前端 + 监控主键）
- `reason`：`ErrorReason` 枚举名（机器判定主键）
- `http_status`：对外 HTTP 状态（语义化）
- `grpc_code`：`google.rpc.Code`（gRPC 必填）

四者在 `errors.proto` 的 option 元信息中声明一次，代码生成时保证一致。

### 2.3 reason 命名规范

- `<DOMAIN>_<MODULE>_<ERROR>`，全大写蛇形
- 示例：`USER_ACCOUNT_NOT_FOUND`、`GATEWAY_UPSTREAM_TIMEOUT`
- 禁止缩写歧义（如 `ERR_*`、`FAIL_*`）

---

## 3. 错误契约（proto 单一事实源）

### 3.1 文件结构

```
template-proto/
  common/
    v1/
      errors.proto        # 通用错误（系统/网关/认证等）
      domains.yaml        # 领域段登记
  user/
    v1/
      user_errors.proto   # 用户域错误
  order/
    v1/
      order_errors.proto  # 订单域错误（示例）
```

### 3.2 Kratos 错误定义（强制使用）

```proto
syntax = "proto3";
package api.user.v1;

import "errors/errors.proto"; // kratos errors 扩展

enum UserErrorReason {
  option (errors.default_code) = 500;

  USER_NOT_FOUND           = 0 [(errors.code) = 404];
  USER_NAME_INVALID        = 1 [(errors.code) = 400];
  USER_ALREADY_EXISTS      = 2 [(errors.code) = 409];
  USER_OPERATION_FORBIDDEN = 3 [(errors.code) = 403];
}
```

通过 `protoc-gen-go-errors` 自动生成：

- `ErrorUserNotFound(format, args...) *errors.Error`
- `IsUserNotFound(err error) bool`

业务代码只调用生成函数，**不允许手写 reason 字符串**。

### 3.3 业务码绑定

为了同时支持 6 位业务码，使用自定义选项扩展（在 `common/v1/errors.proto` 内定义）：

```proto
extend google.protobuf.EnumValueOptions {
  int32  biz_code  = 50001;
  string doc_url   = 50002;
  bool   retryable = 50003;
  string category  = 50004; // business|client|dependency|system
}
```

在枚举定义处附加：

```proto
USER_NOT_FOUND = 0 [
  (errors.code) = 404,
  (biz_code)    = 200101,
  (doc_url)     = "https://docs.internal/errors/200101",
  (retryable)   = false,
  (category)    = "business"
];
```

生成器输出时自动注入 metadata，避免业务侧维护两份信息。

---

## 4. 对外错误响应结构（HTTP / gRPC 双协议统一）

### 4.1 HTTP 响应体（融合 RFC 7807 + Kratos）

```json
{
  "code": 200101,
  "reason": "USER_NOT_FOUND",
  "message": "user not found",
  "type": "https://docs.internal/errors/USER_NOT_FOUND",
  "metadata": {
    "category": "business",
    "retryable": "false",
    "fields": "{\"email\":\"invalid\"}"
  },
  "request_id": "01HE6W...",
  "timestamp": "2026-04-21T09:15:30Z"
}
```

说明：

- `request_id` 放响应 header（`X-Request-Id`）与 body 双写，便于排障
- `type` 对应 `doc_url`，前端/文档可直接跳转
- `metadata` 只放错误自身属性，不放链路信息（trace_id 走 header）
- `fields` 用于结构化校验错误（见 §7）

### 4.2 HTTP 状态码策略（语义化，非“业务一律 200”）

| 类别 | 范围 | HTTP 状态 |
|---|---|---|
| 参数错误 | `*_INVALID_*`、`MISSING_*` | 400 |
| 未认证 | `AUTH_UNAUTHORIZED`、`TOKEN_*` | 401 |
| 无权限 | `*_FORBIDDEN`、`PERMISSION_*` | 403 |
| 未找到 | `*_NOT_FOUND` | 404 |
| 冲突 | `*_CONFLICT`、`*_ALREADY_EXISTS` | 409 |
| 限流 | `*_RATE_LIMITED` | 429 |
| 依赖超时 | `*_UPSTREAM_TIMEOUT` | 504 |
| 依赖不可用 | `*_UPSTREAM_UNAVAILABLE` | 503 |
| 其他系统错误 | `SYSTEM_*` / 未知 | 500 |

落地方式：`http_codec.go` 做**前缀兜底表**映射，不依赖 kratos 默认行为。

### 4.3 gRPC 状态码

- 使用 `google.rpc.Code` 与 HTTP 状态对齐（`INVALID_ARGUMENT`、`NOT_FOUND`、`UNAVAILABLE`…）
- details 使用 `google.rpc.ErrorInfo`（reason + metadata）
- 表单错误附带 `google.rpc.BadRequest.FieldViolation`
- 重试可附带 `google.rpc.RetryInfo`

---

## 5. 分层架构与职责（v2）

```
┌──────────────────────────────┐
│          client / FE         │  按 reason + doc_url 做 UI
├──────────────────────────────┤
│          gateway             │  降维：白名单透出 + 其余收敛
├──────────────────────────────┤
│          service             │  边界映射（errorsx.Map）
├──────────────────────────────┤
│          biz                 │  仅抛业务错误（生成函数）
├──────────────────────────────┤
│          data / infra        │  抛原始技术错误
└──────────────────────────────┘
```

### 5.1 各层规则

- `data/infra`：允许返回 wrapped 原始错误，**不关心 reason**
- `service/边界`：统一 `errorsx.Map(err)`，保证出边界必然是 `*kerrors.Error`
- `biz`：只调用生成的 `v1.ErrorXxx(...)`
- `server`：统一 ErrorEncoder，注入 `request_id/timestamp`
- `gateway`：**降维策略**，避免上游 reason 泄露（见 §8）

### 5.2 “只 wrap 一次”约束

`errorsx.Map` 的首段必须是：

```go
if e, ok := err.(*kerrors.Error); ok {
    return e
}
```

禁止在 service 层使用 `fmt.Errorf("... %w", err)` 再包装 `*kerrors.Error`。

---

## 6. 目录落位（跨仓最终形态）

### 6.1 `template-proto`

- `common/v1/errors.proto`：通用错误 + 自定义选项
- `common/v1/domains.yaml`：领域段登记表
- `<domain>/v1/<domain>_errors.proto`：各领域错误

### 6.2 `kratos-template`

- `api/...`：proto 生成物（含 `Error*/Is*` 函数）
- `internal/errorsx/`：
  - `mapper.go`：`Map(err)` 分类映射
  - `assert.go`：测试辅助（`AssertReason`、`AssertCategory`）
  - `logging.go`：按 category 分级日志策略
- `internal/server/http_codec.go`：统一 ErrorEncoder
- `internal/server/grpc_interceptor.go`（新增）：gRPC 错误拦截
- `biz/service` 代码中直接用生成函数

### 6.3 `kratos-gateway`

- `internal/errorsx/`：
  - `reducer.go`：降维白名单
  - `http_encoder.go`：统一网关输出
- `internal/middleware/retry.go`（可选）：按 `metadata.retryable` 决定重试

### 6.4 `kratos-infra`

- `errorsx/classifier.go`：`IsTimeout/IsCanceled/IsNetwork/IsUnavailable`
- `errorsx/reason.go`：**仅通用 reason**（如 `SYSTEM_INTERNAL_ERROR`）
- `middleware/auth`、`middleware/recovery`：改为返回生成的标准错误
- **废弃**：`kratos-infra/errors/errors.go` 的裸哨兵对外使用

---

## 7. 结构化校验错误（表单场景）

参考 `google.rpc.BadRequest`：

```json
{
  "code": 100001,
  "reason": "COMMON_VALIDATION_FAILED",
  "message": "request validation failed",
  "metadata": {
    "fields": "[{\"field\":\"email\",\"reason\":\"INVALID_FORMAT\"},{\"field\":\"age\",\"reason\":\"TOO_SMALL\"}]"
  }
}
```

要求：

- 新增 `COMMON_VALIDATION_FAILED` 作为总入口 reason
- `metadata.fields` 使用固定 JSON Schema，前端按字段展示
- 禁止把多字段错误拆成多次请求响应

---

## 8. 网关错误降维（v2 新增关键机制）

### 8.1 白名单透出

- gateway 维护 `allowed_reasons.yaml`：允许对外透出的 reason 集合
- 命中白名单：原样透出 code/reason/message
- 未命中：
  - `category=system` → 收敛为 `GATEWAY_UPSTREAM_ERROR`（500/502）
  - `category=dependency` → `GATEWAY_UPSTREAM_UNAVAILABLE`（503）
  - 其他 → `GATEWAY_UNKNOWN_ERROR`（500）

### 8.2 好处

- 前端只需要处理稳定的对外 reason 集合
- 内部细分 reason 不会污染公共契约
- 避免敏感信息通过 message 泄漏

---

## 9. 重试与幂等语义（新增）

### 9.1 元数据约定

- `metadata.retryable`：`true|false`
- `metadata.retry_after_ms`：建议等待毫秒数（可选）
- `metadata.idempotency_safe`：`true|false`（POST 场景尤需）

### 9.2 客户端策略

- `retryable=true` 且 `category=dependency` → 自动重试（带指数退避）
- `retryable=false` → 立即失败
- `retry_after_ms` 存在时优先使用

### 9.3 gateway 策略

- 对空闲请求（GET）可以直接按 `retryable` 决策
- 对写操作仅当 `idempotency_safe=true` 时才允许重试

---

## 10. 观测与 SRE 对齐

### 10.1 日志规范

错误日志必带：

- `trace_id`, `request_id`
- `service`, `method`
- `code`, `reason`, `category`
- `http_status` / `grpc_code`
- `upstream`（若有）
- `latency_ms`

### 10.2 分级策略（按 category）

- `business` / `client`：`Info` + 采样（默认 10%）
- `dependency`：`Warn` 全量
- `system`：`Error` 全量 + stack

### 10.3 指标

```
app_error_total{service,reason,code,category}
app_http_error_total{service,route,http_status}
app_dependency_error_total{service,dependency,reason}
app_gateway_downgrade_total{reason_original,reason_final}
```

### 10.4 SLO 对齐

- 将 `category=system` 计入错误预算
- `category=business` 不计入 SLO（业务错误 ≠ 故障）
- 告警示例：
  - `category=system` 错误预算 24h 内消耗 > 2% → 告警
  - `GATEWAY_UPSTREAM_TIMEOUT` QPS > 阈值 → 告警

---

## 11. 安全与合规

- message 仅返回用户可见信息，禁止包含：
  - 栈信息、文件路径
  - SQL/Redis 原始错误
  - 第三方服务原始错误信息
- 内部错误在日志保留完整上下文，响应中只返通用文案
- 对未授权访问统一返回 `401 AUTH_UNAUTHORIZED`，不泄露资源是否存在

---

## 12. 国际化（i18n）

- 服务端返回固定 `reason` 与默认 `message`
- 前端/BFF 以 `reason` 为 key 做 i18n 映射
- 可选：网关支持 `Accept-Language` 返回多语 `message`（二阶段）

```json
{ "USER_NOT_FOUND": { "zh-CN": "用户不存在", "en-US": "User not found" } }
```

---

## 13. 治理机制（CI / 评审 / 冻结）

### 13.1 CI 检查

- `errors.proto` diff 校验：禁止修改已发布 code/reason
- 全仓扫描：禁止硬编码 `kerrors.BadRequest("XXX"...)`
- 检查 reason 是否命中生成文件中的常量
- 校验领域段是否越界使用

### 13.2 Review 规则

- 新增错误 PR 必须：
  - 更新 `errors.proto`
  - 更新 `allowed_reasons.yaml`（若需对外）
  - 补充测试（`AssertReason`）

### 13.3 冻结策略

- 已发布版本的 `errors.proto` 做 snapshot
- 下一次 PR 对比 snapshot，禁止破坏性修改
- 废弃使用 `[deprecated = true]` 标记，保留号段

---

## 14. 测试规范

- `biz` 每条错误路径必须有测试
- 必须断言 **reason**，而非 `assert.Error`：

```go
errorsx.AssertReason(t, err, v1.UserErrorReason_USER_NOT_FOUND)
```

- 集成测试断言响应体中的 `code/reason/http_status`
- gateway 降维逻辑必须单独覆盖测试

---

## 15. 与现有仓库的精准改造映射

### 15.1 `template-proto`

- 新增：`common/v1/errors.proto`、`user/v1/user_errors.proto`
- 接入：`protoc-gen-go-errors`

### 15.2 `kratos-template`

- 替换：`internal/biz/user.go` 中 `kerrors.BadRequest("INVALID_USER_ID"...)` → `v1.ErrorUserIdInvalid(...)`
- 替换：`internal/service/user.go` 中 `kerrors.BadRequest("INVALID_UPDATE_FIELDS"...)` → `v1.ErrorUserUpdateFieldsEmpty(...)`
- 改造：`internal/server/http_codec.go` 输出 v2 响应体结构（`code/reason/message/type/metadata/request_id/timestamp`）
- 新增：`internal/server/grpc_interceptor.go` 统一 gRPC 错误出站
- 新增：`internal/errorsx/{mapper,assert,logging}.go`

### 15.3 `kratos-gateway`

- 新增：`internal/errorsx/{reducer,http_encoder}.go`
- 新增：`configs/allowed_reasons.yaml`
- 改造：HTTP server 挂载统一 ErrorEncoder
- 可选：`internal/middleware/retry.go`（按 metadata 驱动）

### 15.4 `kratos-infra`

- 改造：`middleware/auth/auth.go` 返回生成的标准错误（`AuthErrorReason_UNAUTHORIZED` 等）
- 改造：`middleware/recovery/recovery.go` 返回 `SYSTEM_INTERNAL_ERROR`
- 拆分：`errorsx/classifier.go`（新）+ 旧 `errors/errors.go` 下线对外使用

---

## 16. 分阶段实施计划（5 周）

### Phase 0（准备，2 天）

- 统一领域段，登记 `domains.yaml`
- 选型确认：`protoc-gen-go-errors` 集成到 Makefile

### Phase 1（契约与框架，第 1 周）

- 落地 `errors.proto` 与生成链
- 新增 `internal/errorsx`、`http_codec` 输出 v2 结构
- 完成基础 mapper：gorm / redis / grpc / context

### Phase 2（服务收敛，第 2 周）

- 替换 `kratos-template` 全部业务错误
- 单元测试全部用 `AssertReason`
- 与前端联调确认响应结构

### Phase 3（infra 与 gateway，第 3 周）

- 中间件错误改造
- 网关降维与白名单
- 下线裸哨兵错误对外使用

### Phase 4（观测与治理，第 4 周）

- 指标与 Dashboard
- 告警规则
- CI 冲突检测与冻结脚本

### Phase 5（SLO 与运营，第 5 周）

- 定义 category=system 的错误预算
- 错误码文档站上线（基于 `doc_url`）
- 团队流程固化到 Contribution Guide

---

## 17. 验收清单（可逐项 check）

- [ ] 所有错误均由 `protoc-gen-go-errors` 生成函数构造
- [ ] 代码库中不存在硬编码 reason 字符串
- [ ] HTTP 响应结构包含 `code/reason/message/type/metadata/request_id/timestamp`
- [ ] gRPC 错误使用 `google.rpc.ErrorInfo`
- [ ] gateway 对非白名单 reason 全部降维
- [ ] 所有错误日志包含 `trace_id/request_id/category`
- [ ] `system` 类错误接入错误预算与告警
- [ ] CI 拒绝破坏性修改 `errors.proto`
- [ ] 结构化校验错误覆盖核心表单接口
- [ ] 所有 biz error 路径有 `AssertReason` 测试

---

## 18. 风险与规避

- 风险：`protoc-gen-go-errors` 学习成本  
  规避：提供样例 `user_errors.proto` + Makefile 一键生成

- 风险：gateway 降维可能误伤业务 reason  
  规避：白名单默认宽松，新增服务时先放开再逐步收敛

- 风险：团队回退到硬编码 reason  
  规避：CI 扫描 + Code Review + 错误码文档站双重治理

- 风险：message 国际化带来工期延长  
  规避：首阶段只做 reason 契约，i18n 交给前端

---

## 19. 与 v1 的差异摘要

- 新增：自动生成错误函数（禁止手写）
- 新增：业务码 + HTTP + gRPC 四要素同源机制
- 新增：网关降维与白名单
- 新增：结构化校验错误
- 新增：metadata 标准字段（retryable、category、retry_after_ms、idempotency_safe）
- 新增：SLO/错误预算对齐
- 新增：只 wrap 一次、分级日志、CI 冻结机制
- 强化：安全与合规对 message 的约束
- 强化：测试必须断言 reason

---

## 20. 交付物清单

- `template-proto/common/v1/errors.proto` + 领域错误 proto
- `template-proto/common/v1/domains.yaml`
- `kratos-template/internal/errorsx/*`
- `kratos-template/internal/server/http_codec.go`（v2 格式）
- `kratos-template/internal/server/grpc_interceptor.go`
- `kratos-gateway/internal/errorsx/*`
- `kratos-gateway/configs/allowed_reasons.yaml`
- `kratos-infra/errorsx/classifier.go`
- CI 规则脚本 + 错误码文档站
- 本文档与 v1 文档共同作为执行基线

---

## 21. 最终建议

- 这是一次 **架构级** 重构，不要只当成代码整理
- 关键是把“错误码”当成**产品契约**来管理，而不是技术细节
- 一旦 Phase 1 完成，后续所有服务都应按此契约新建，无例外
- 当团队规模增长时，统一错误协议带来的收益会非线性放大

> 执行建议：从 `kratos-template` 的 user 域开始试点，跑通一个完整的 Phase 1+2，再横向复制到 gateway 与其他服务。
