# api-contracts 方案对比与执行文档

本文档用于统一以下目标：
- 多服务、多人并行开发，协议治理不混乱；
- proto 定义与语言生成代码分离；
- 保证 proto 强一致性（版本、生成链路、发布流程可审计）。

## 一、结论总览

### 方案1：分仓（推荐长期治理）
- 仓库拆分：
  - `api-contracts-proto`：只放 `.proto` 与 buf 治理配置；
  - `api-contracts-go`：只放 Go 生成代码与 `go.mod`；
  - `api-contracts-java`：只放 Java 生成代码与构建配置（可选）。
- 适用场景：
  - 团队规模较大；
  - 多语言客户端较多；
  - 需要明确协议组和业务组职责边界。
- 优点：
  - 职责最清晰；
  - 代码评审噪音最低；
  - 语言发布节奏可独立。
- 代价：
  - 仓库数量增加；
  - 需要自动化保证跨仓同版本发布。

### 方案2：单仓分目录（推荐当前阶段）
- 单仓保留为 `api-contracts`；
- 目录分离：
  - `api/`：仅 proto；
  - `gen/go/`：仅 Go 生成代码；
  - `gen/java/`：仅 Java 生成代码（可逐步接入）。
- 适用场景：
  - 希望兼顾治理与开发效率；
  - 暂不想承担多仓维护成本。
- 优点：
  - 一次改动在一个 PR 内闭环；
  - 治理规则可直接沿用；
  - 落地成本低。
- 代价：
  - 仓库体积会随生成物增长；
  - 多语言发布策略要在单仓内规范化。

### 方案3：纯 proto 仓，生成放到服务仓执行
- `api-contracts` 仅维护 proto；
- 每个业务服务在本仓执行 `buf generate`。
- 优点：
  - 协议仓最干净；
  - 服务开发就地生成，改动直观。
- 风险：
  - 本地工具版本不统一会引发 CI 与本地不一致；
  - 生成链路分散，治理成本高于方案1/2。

## 二、三种方案如何保证强一致性

无论选择哪种方案，必须同时满足以下约束：

1. 单一协议真源  
   只能在一个地方修改 proto（协议仓或单仓 `api/`）。

2. 版本锚点  
   所有服务只依赖明确版本（Git tag / 制品版本），禁止直接依赖 `main`。

3. 破坏性变更门禁  
   PR 必须通过：
   - `buf lint`
   - `buf breaking --against '.git#branch=main'`

4. 生成链路可复现  
   固定 buf 与插件版本，CI 使用固定镜像或固定工具版本执行生成。

5. 发布策略清晰  
   协议发布后，业务服务按需升级版本，禁止强制同步升级所有服务。

## 三、落地规范（团队约定）

1. 目录规范
- 领域分组，不按服务分组：
  - `api/user/v1`
  - `api/order/v1`
  - `api/payment/v1`
- 版本只允许大版本目录升级：`v1` -> `v2`。

2. 变更规范
- 允许：
  - 新增字段；
  - 新增 RPC；
  - 新增 message。
- 禁止（破坏兼容）：
  - 修改字段编号；
  - 修改字段类型；
  - 直接删除字段（只能 deprecated）。

3. 流程规范
- proto 变更流程：
  1) 提 PR；
  2) 通过 lint/breaking；
  3) 合并并打 tag；
  4) 业务服务按需升级依赖。

4. 服务引用规范（Kratos/Go）
- 服务禁止跨仓 import 其他服务 `internal`；
- 服务仅通过协议生成包进行编译与通信；
- 服务依赖升级通过 `go get module@version` 完成。

## 四、当前执行决策

基于当前阶段，先执行：

1) 当前 `api-contracts` 使用方案2（单仓分目录）；  
2) 在仓库根目录新增一个方案1示例目录，用于后续拆仓演练与发布流程验证。

## 五、后续可扩展项

- 增加 Java 生成插件与制品发布流水线；
- 增加错误码规范仓（跨服务一致的 error code）；
- 接入 BSR 或内部制品中心，统一协议发布与追踪。

