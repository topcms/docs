# ESXi 资源优化与救援操作手册

## 1. 背景与目标

当前宿主机为 4 逻辑 CPU（Intel N5105）+ 32GB 内存，运行 7 台虚拟机，已出现 `pro_uat` 卡死后导致整体管理不可达的问题。  
本手册目标：

- 最大化资源利用率，同时避免单台虚拟机拖垮整机
- 保证 `openwrt` 稳定在线，避免远程完全失联
- 建立可重复执行的远程救援路径与演练流程

---

## 2. 现状与核心风险

### 2.1 资源超配风险

- vCPU 总分配：19 vCPU（显著高于宿主 4 逻辑核）
- 内存总分配：35GB（高于物理 32GB，且 ESXi 本身需要内存）

结论：CPU 抢占和内存压力叠加时，容易触发宿主管理面无响应（`hostd` 卡顿/超时），表现为 ESXi 失联。

### 2.2 已确认网络现状

- `win2016` 有双网卡：
  - `NIC1`：`openwrt` 内网（当前为 DHCP）
  - `NIC2`：外部网络，且与 ESXi 管理网络同级
- 已验证：`win2016` 通过 `NIC1` 可以 ping 通 ESXi 管理 IP

结论：你已具备可行救援链路，但仍需完善配置以避免关键时刻失效。

---

## 3. 目标架构与分级策略

## 3.1 服务分级

- **T0（救援核心）**：`openwrt`、`win2016`
- **T1（长期核心）**：`jumpserver`
- **T2（长期业务）**：`data_os`、`pro_uat`、`tool_uat`
- **T3（按需开启）**：`ts_uat`

### 3.2 管理与业务平面分离

- ESXi 管理平面优先走外部网络（与 `win2016 NIC2` 同段）
- 业务流量走 `openwrt` 内网
- 故障时通过 `win2016 NIC2 -> ESXi 管理` 进行兜底操作

---

## 4. 推荐资源配置（可直接落地）

## 4.1 虚拟机资源建议表

| VM | vCPU | 内存 | CPU 预留 | CPU 限制 | CPU 份额 | 内存预留 | 启动策略 |
|---|---:|---:|---:|---:|---|---:|---|
| openwrt | 1 | 1G | 500 MHz | 不限制 | 高 | 1G（100%） | 自动，0s |
| win2016 | 2 | 3G | 500 MHz | 不限制 | 高 | 3G（建议） | 自动，15s |
| jumpserver | 2 | 3G | 300 MHz | 不限制 | 高 | 2G（建议） | 自动，30s |
| data_os | 2 | 6G | 0 | 不限制 | 正常 | 4G（建议） | 自动，60s |
| pro_uat | 2 | 6G | 0 | 3000 MHz | 正常 | 4G（建议） | 自动，90s |
| tool_uat | 2 | 6G | 0 | 2500 MHz | 低 | 2G（建议） | 自动，120s |
| ts_uat | 2 | 2G | 0 | 1500 MHz | 低 | 0 | 手动 |

### 4.2 关键原则

- `pro_uat` 从 4 vCPU 降到 2 vCPU，避免单机吃满 4 核
- 为高风险高负载机设置 `CPU Limit`（`pro_uat`、`tool_uat`）
- `openwrt` 内存做 100% 预留，保证网关不会被挤压
- `ts_uat` 改为按需启动，释放常驻资源

---

## 5. win2016 双网卡规范（救援稳定性关键）

### 5.1 NIC2（外网，救援入口）

- 保持与 ESXi 管理网络同级
- 使用固定地址（静态 IP 或路由器 DHCP 绑定）
- 仅该网卡保留默认网关

### 5.2 NIC1（内网，运维跳板）

- 建议由 DHCP 改为静态 IP（从内网地址池外选一个固定地址）
- **不要配置默认网关**
- 用于访问内网虚拟机与 openwrt 管理地址

### 5.3 路由原则

- `0.0.0.0/0` 默认路由只允许存在一条（走 NIC2）
- 避免双默认网关导致 RDP 回程路由漂移

---

## 6. ESXi 启动顺序建议

- `openwrt`：0s
- `win2016`：15s
- `jumpserver`：30s
- `data_os`：60s
- `pro_uat`：90s
- `tool_uat`：120s
- `ts_uat`：手动

作用：宿主重启后，先恢复网络与救援入口，再逐步拉起业务。

---

## 7. 故障应急流程（pro_uat 卡死场景）

### 7.1 现象

- 内网服务不可达，可能伴随 openwrt 异常
- ESXi Web 管理响应慢或不可访问

### 7.2 远程处置路径（优先）

1. 从外网 RDP 登录 `win2016`（走 `NIC2`）
2. 在 `win2016` 访问 ESXi 管理页面
3. 定位并强制关闭 `pro_uat`
4. 确认 `openwrt`、`jumpserver` 恢复
5. 再次启动 `pro_uat` 并检查其业务日志

### 7.3 SSH 兜底（ESXi）

当 Web UI 无响应时，使用 SSH：

```bash
esxcli vm process list
esxcli vm process kill -t soft -w <WorldID>
esxcli vm process kill -t hard -w <WorldID>
esxcli vm process kill -t force -w <WorldID>
```

---

## 8. 演练清单（建议每月一次）

1. 关机 `openwrt`（维护窗口内）
2. 验证是否可从外网 RDP 到 `win2016 NIC2`
3. 验证 `win2016` 可访问 ESXi 管理页面
4. 验证能否通过 ESXi 控制台管理其它 VM
5. 启动 `openwrt` 并验证业务恢复
6. 记录耗时与问题，更新应急手册

---

## 9. 落地顺序（一次性执行）

1. 调整 `pro_uat`：`4vCPU -> 2vCPU`，设置 `CPU Limit=3000 MHz`
2. 调整 `tool_uat`：设置 `CPU Limit=2500 MHz`
3. 调整 `openwrt`：内存 100% 预留，CPU 预留 500MHz，份额高
4. 调整 `ts_uat`：改为手动启动
5. 固化 `win2016` 双网卡策略（NIC1 静态无网关，NIC2 作为默认网关）
6. 配置 ESXi 自动启动顺序
7. 完整跑一遍演练清单

---

## 10. 验收标准

满足以下条件即视为完成：

- 单台 VM（尤其 `pro_uat`）高负载时，ESXi 管理面仍可访问
- `openwrt` 波动时，仍可通过 `win2016 NIC2` 进入救援链路
- 宿主内存不再长期超配，且无明显持续性 swap 症状
- 每次故障可在 5 分钟内完成远程止血

