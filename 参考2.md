下面给你一套 **可以直接落地的 Kratos 企业级脚手架模板**。
目标：新服务 5 分钟创建 → 一键生成代码 → 一键构建 → 一键发版。

这套模板已经包含：

* Makefile 自动化
* proto 生成流水线
* wire 依赖注入
* Docker 多阶段构建
* CI/CD 模板
* 标准目录结构
* 本地开发环境

---

# 一、最终脚手架结构

以后每个新服务都是这一套：

```
kratos-template/
├── api/                # 引用 proto 生成代码（只放生成物）
├── cmd/server/         # main
├── internal/
│   ├── biz/
│   ├── data/
│   ├── service/
│   ├── server/
│   └── conf/
│
├── configs/
│   ├── config.yaml
│   └── registry.yaml
│
├── build/
│   ├── docker/
│   │   └── Dockerfile
│   └── k8s/
│       └── deployment.yaml
│
├── third_party/        # google/api 等
├── Makefile
├── wire.go
├── wire_gen.go
└── go.mod
```

---

# 二、Makefile（核心自动化）

企业项目必须用 Makefile 统一所有操作。

## Makefile

```makefile
APP_NAME := user-service
REGISTRY := registry.company.com
VERSION  := $(shell git describe --tags --always)

.PHONY: init
init:
	go mod tidy

.PHONY: api
api:
	buf generate

.PHONY: wire
wire:
	cd cmd/server && wire

.PHONY: build
build:
	mkdir -p bin
	go build -ldflags "-X main.Version=$(VERSION)" -o ./bin/ ./cmd/server

.PHONY: run
run:
	go run ./cmd/server

.PHONY: docker
docker:
	docker build -t $(REGISTRY)/$(APP_NAME):$(VERSION) -f build/docker/Dockerfile .

.PHONY: push
push:
	docker push $(REGISTRY)/$(APP_NAME):$(VERSION)

.PHONY: all
all: api wire build docker push
```

开发者以后只记住：

```
make api
make wire
make build
make docker
```

---

# 三、Proto 自动生成（buf 方案）

比 kratos proto 更适合企业。

## 安装

```
go install github.com/bufbuild/buf/cmd/buf@latest
```

---

## buf.yaml

```
version: v1
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
```

---

## buf.gen.yaml

```
version: v1
plugins:
  - plugin: go
    out: .
    opt: paths=source_relative

  - plugin: go-grpc
    out: .
    opt: paths=source_relative

  - plugin: go-http
    out: .
    opt: paths=source_relative

  - plugin: openapi
    out: .
```

---

## 生成命令

```
make api
```

团队统一生成方式。

---

# 四、Wire 依赖注入模板

Kratos 必须用 wire，否则项目会变乱。

## cmd/server/wire.go

```go
//go:build wireinject
// +build wireinject

package main

import (
	"github.com/google/wire"
)

func wireApp() (*App, error) {
	panic(wire.Build(
		newApp,
		server.ProviderSet,
		data.ProviderSet,
		biz.ProviderSet,
		service.ProviderSet,
	))
}
```

---

## ProviderSet 示例

### data/provider.go

```go
var ProviderSet = wire.NewSet(
	NewDB,
	NewRedis,
	NewUserRepo,
)
```

### biz/provider.go

```go
var ProviderSet = wire.NewSet(
	NewUserUsecase,
)
```

### service/provider.go

```go
var ProviderSet = wire.NewSet(
	NewUserService,
)
```

---

生成依赖：

```
make wire
```

---

# 五、Docker 企业级构建（多阶段）

## build/docker/Dockerfile

```dockerfile
FROM golang:1.22 AS builder

WORKDIR /app
COPY . .
RUN go mod download
RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o server ./cmd/server

FROM alpine:3.19

WORKDIR /app
COPY --from=builder /app/server .
COPY configs ./configs

EXPOSE 8000
EXPOSE 9000

CMD ["./server", "-conf", "./configs"]
```

优势：

* 最终镜像 < 30MB
* 不带源码
* 不带 go toolchain

---

# 六、配置文件模板

## configs/config.yaml

```yaml
server:
  http:
    addr: 0.0.0.0:8000
  grpc:
    addr: 0.0.0.0:9000

data:
  database:
    driver: mysql
    source: root:pass@tcp(mysql:3306)/test

  redis:
    addr: redis:6379
```

---

# 七、K8S 部署模板

## build/k8s/deployment.yaml

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: app
          image: registry.company.com/user-service:latest
          ports:
            - containerPort: 8000
            - containerPort: 9000
```

---

# 八、CI/CD 模板（GitLab）

## .gitlab-ci.yml

```yaml
stages:
  - test
  - build
  - docker
  - deploy

test:
  stage: test
  script:
    - go vet ./...
    - go test ./...

build:
  stage: build
  script:
    - make build

docker:
  stage: docker
  script:
    - make docker
    - make push

deploy:
  stage: deploy
  script:
    - kubectl apply -f build/k8s/deployment.yaml
```

---

# 九、新服务创建流程（真正落地）

以后团队新建服务：

```
1️⃣ 从模板创建仓库
2️⃣ 修改 APP_NAME
3️⃣ 修改 proto 引用
4️⃣ make all
5️⃣ 推送仓库
```

一个服务半小时上线。

---

# 十、企业级开发日常流程

开发只需要记住：

```
新增接口 → 修改 proto → make api
写业务 → make wire
本地跑 → make run
发布 → make all
```

---

# 十一、这套模板解决了什么

| 问题       | 已解决             |
| -------- | --------------- |
| proto 混乱 | buf统一生成         |
| 依赖注入混乱   | wire            |
| 构建混乱     | Makefile        |
| 部署混乱     | Docker + K8S    |
| CI不统一    | gitlab pipeline |

---

如果你愿意，我可以继续帮你补：
👉 **api-contracts 仓库设计 + proto 版本治理策略**（这是下一步最关键的）。
