这一块非常关键。
没有统一错误码，微服务最终会变成：

* 前端无法统一处理错误
* 日志无法聚合
* 监控无法统计
* 排障只能看日志

**错误码 ≈ 微服务的语言**

下面给你一套可以直接落地的企业级方案。

---

# 一、错误码体系的目标

统一错误体系要解决 5 件事：

1. 前端能统一处理错误
2. 服务间能识别错误来源
3. 日志可聚合统计
4. 告警可自动化
5. 错误不会冲突

---

# 二、错误码总体结构设计

企业推荐使用 **6 位数字错误码**

```text
AA BB CC
```

含义：

| 段  | 含义   | 示例                |
| -- | ---- | ----------------- |
| AA | 系统级  | 10=网关 20=用户 30=订单 |
| BB | 模块级  | 01=登录 02=资料 03=权限 |
| CC | 具体错误 | 01=不存在 02=参数错误    |

示例：

```text
200101
```

含义：

* 20 → 用户域
* 01 → 登录模块
* 01 → 用户不存在

---

# 三、系统级编号分配（公司级固定）

必须全公司统一登记：

```text
10 网关 gateway
20 用户 user
30 订单 order
40 支付 payment
50 内容 content
60 IM
70 游戏
80 后台 admin
90 基础设施 infra
```

以后新增领域 → 申请编号。

避免冲突。

---

# 四、错误码仓库（error-codes）

建议单独仓库：

```text
error-codes/
└── errors.yaml
```

## errors.yaml

```yaml
user:
  login:
    200101: USER_NOT_FOUND
    200102: PASSWORD_WRONG
    200103: USER_BANNED

  profile:
    200201: NICKNAME_INVALID
```

这是公司级错误字典。

---

# 五、错误返回结构（proto 统一）

所有服务必须返回统一错误模型。

```proto
message Error {
  int32 code = 1;
  string reason = 2;
  string message = 3;
  map<string,string> metadata = 4;
}
```

解释：

| 字段       | 用途   |
| -------- | ---- |
| code     | 机器识别 |
| reason   | 错误常量 |
| message  | 给用户看 |
| metadata | 调试信息 |

---

# 六、Kratos 错误封装规范

统一封装错误创建函数。

## internal/errors/errors.go

```go
func New(code int, reason string, msg string) *errors.Error {
    return errors.New(
        int32(code),
        reason,
        msg,
    )
}
```

---

# 七、每个服务的错误定义方式

每个服务维护自己的错误文件：

```text
internal/errors/user.go
```

```go
var (
    ErrUserNotFound = errors.New(200101, "USER_NOT_FOUND", "用户不存在")
    ErrPasswordWrong = errors.New(200102, "PASSWORD_WRONG", "密码错误")
)
```

禁止写 magic number：

```go
return errors.New(12345, ...)
```

必须引用常量。

---

# 八、HTTP 映射规则（非常关键）

统一 HTTP 状态码映射：

| 错误类型  | HTTP |
| ----- | ---- |
| 参数错误  | 400  |
| 未登录   | 401  |
| 无权限   | 403  |
| 资源不存在 | 404  |
| 业务错误  | 200  |
| 系统错误  | 500  |

注意：

**业务错误返回 200**

因为：

* 前端只看业务 code
* 避免被网关吞掉

---

# 九、服务间错误传递

微服务 A 调 B：

禁止这样：

```go
return err
```

必须转换为领域错误：

```go
if errors.Is(err, user.ErrUserNotFound) {
    return order.ErrBuyerNotFound
}
```

否则会出现：

* 错误语义泄漏
* 服务强耦合

---

# 十、日志规范（与错误码绑定）

所有 error log 必须打印：

```json
{
  "trace_id":"xxx",
  "error_code":200101,
  "reason":"USER_NOT_FOUND"
}
```

这样监控系统才能统计：

* 今日 USER_NOT_FOUND 次数
* 哪个接口最多错误

---

# 十一、监控与告警

Prometheus 统计：

```text
error_total{code="200101"} 5321
```

告警示例：

```text
USER_NOT_FOUND > 1000/min → 告警
```

这就是企业级可观测。

---

# 十二、多语言前端国际化

前端只根据 code 映射文案：

```json
{
  "200101": "User not found"
}
```

服务端不做多语言。

---

# 十三、错误码生命周期

错误码一旦发布：

* 永远不能删除
* 只能废弃
* 永远不能复用

否则旧客户端会崩。

---

# 十四、最终团队流程

新增错误流程：

```text
1. 在 error-codes 仓库申请编号
2. PR 审核
3. 服务使用该错误码
```

---

# 十五、总结

统一错误码带来的收益：

| 能力     | 是否获得 |
| ------ | ---- |
| 前端统一处理 | ✔    |
| 日志聚合   | ✔    |
| 告警自动化  | ✔    |
| 跨服务排障  | ✔    |

---

如果你继续往企业级走，下一步就是：
**服务网关 / BFF 统一设计（Gateway 架构）**。
